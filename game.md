## 게임 제목

# 1. 웃긴 모양의 캐릭터가 노래부르는 모습으로 인트로를 만들면서 게임 시작 버튼이 활성화 된다

# 2. 게임 항목이 4개 존재, 첫번쨰는 자음과 모음을 합쳐서 누를때마다 소리나는 게임

- 첫번째 게임  (자음과 모음 합치기) 

소리는 웃긴 캐릭터의 목소리로 수행한다.

[자음]
ㄱ ㄴ ㄷ ㄹ ㅁ ㅂ ㅅ ㅇ ㅈ ㅊ ㅋ ㅌ ㅍ ㅎ
[모음]
ㅏ ㅑ ㅓ ㅕ ㅗ ㅛ ㅜ ㅠ ㅡ ㅣ 
자음과 모음 누를떄마다 소리가 나고 자음과 모음이 합쳐지면 만들어지는 한글과 이미지가 보인다.

- 두번쨰 게임 
ㄱ ㄴ ㄷ ㄹ ㅁ ㅂ ㅅ ㅇ ㅈ ㅊ ㅋ ㅌ ㅍ ㅎ 로 이쁜 한글 버튼이 만들어지고 , 이 버튼을 누르면 쉬운 단어가 사진과 함께 만들어진다.

- 세번째 게임
알아서 쉽게 만들어주세요

- 네번째 게임
한글을 던지면 그 한글이 뜻하는 것들이 그림으로 만들어지게 해주세요

모든 게임에는 홈으로 돌아가기 버튼이 있다.

## 한글 게임 개발 컨벤션 (Convention Rules)

1. **디렉토리 및 파일 구조**
   - 각 게임은 `src/pages/game{N}/` 폴더에 독립적으로 구성한다.
   - CSS는 반드시 CSS Modules(`*.module.css`)를 사용하여 다른 게임과 스타일 충돌을 방지한다.
   - 공통 스타일(버튼, 배경 등)이 필요하다면 `Game.module.css` 등에 정의하고 재사용한다.

2. **상태 관리 및 네이밍 규칙**
   - React `useState`를 사용하여 상태를 관리한다.
   - 이벤트 핸들러 함수는 `handle{Action}` 형태로 명명한다. (예: `handleSelect`, `handleThrow`)
   - 상태 변수는 직관적으로 명명한다. (예: `isThrowing`, `showResult`)

3. **사운드 및 TTS (Text-to-Speech) 제어**
   - 외부 API 대신 브라우저 내장 `window.speechSynthesis` API를 활용한다.
   - 사운드가 겹치지 않도록 재생 전 항상 `window.speechSynthesis.cancel()`을 호출한다.
   - `pitch`(음높이)와 `rate`(속도)를 조절하여 각 게임의 테마(귀여운, 웃긴, 경쾌한)에 맞는 목소리를 연출한다.

4. **UI/UX 및 애니메이션**
   - 타겟층(아이들)에 맞추어 폰트는 `Jua` (구글 폰트)를 기본으로 적용한다.
   - 클릭 가능한 요소(버튼, 카드)는 마우스 호버 시 살짝 커지는 효과(`transform: scale(...)`)를 부여하여 상호작용을 명확히 한다.
   - 이벤트 및 결과 발생 시 CSS `@keyframes` (예: `bounceIn`, `shake`)를 사용하여 시각적 피드백을 극대화한다.
   - 의미 없는 단일 자음/모음 조합의 경우 혼란을 피하기 위해 임의의 이모지를 렌더링하지 않는다.
